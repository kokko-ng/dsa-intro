Commit and push my latest changes to the private repo.

Instructions:
1. Ensure you are on the `kokko` branch (create if needed: `git checkout -b kokko`)
2. Stage all changes: `git add -A`
3. Commit with message: Use $ARGUMENTS if provided, otherwise auto-generate descriptive message
4. Push to private remote only: `git push kokko-priv kokko`

IMPORTANT: Never push to origin. Only push to the kokko-priv remote.
